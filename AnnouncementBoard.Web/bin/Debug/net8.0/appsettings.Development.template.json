{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "// NOTE": "All sensitive configuration (Google OAuth, DB passwords) are stored in User Secrets.",
  "// TO SET UP": "Run these commands:",
  "// ": "dotnet user-secrets set 'GoogleAuth:ClientId' 'your-google-client-id'",
  "// ": "dotnet user-secrets set 'GoogleAuth:ClientSecret' 'your-google-client-secret'",
  "// ": "dotnet user-secrets set 'ConnectionStrings:TaskboardDB' 'your-postgresql-connection-string'"
}
